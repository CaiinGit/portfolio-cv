services:
  portfolio:
    image: nginx:alpine
    container_name: portfolio-cv
    restart: unless-stopped
    ports:
      - "192.168.1.197:8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
